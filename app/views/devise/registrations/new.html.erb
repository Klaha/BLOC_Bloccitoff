<div class="row">
  <div class="col s12 center-align">
    <h2 class="hide-on-small-only">Sign Up</h2>
    <h3 class="hide-on-med-and-up">Sign Up</h3>
    <div class="row">
      <div class="col s12 red-text">
        <%= devise_error_messages! %>
      </div>
    </div>
  </div>
</div>
<div class="row">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), :html => {class: 'col s12 m10 offset-m1 l10 offset-l1'}) do |f| %>
      <div class="row">
        <div class="input-field col s12">
          <%= f.label :email %>
          <%= f.email_field :email, class: 'validate' %>
        </div>
      </div>
      <div class="row">
        <div class="input-field col s12">
          <%= f.label :password, 'Password (8 chars minimum)' %>
          <%= f.password_field :password, autocomplete: "off", class: 'validate' %>
        </div>
      </div>
      <div class="row">
        <div class="input-field col s12">
          <%= f.label :password_confirmation %>
          <%= f.password_field :password_confirmation, autocomplete: "off", class: 'validate' %>
        </div>
      </div>
      <div class="row">
        <div class="actions col s12 center-align">
          <button class="btn waves-effect waves-light" type="submit" name="action">Submit
            <i class="mdi-content-send right"></i>
          </button>
          <br>
          <p class="center-align"><%= render "devise/shared/links" %></p>  
        </div>
      </div>
    <% end %>
  </div>


